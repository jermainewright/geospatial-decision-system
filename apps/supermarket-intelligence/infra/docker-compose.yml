version: "3.9"
services:
  api:
    build: ../backend
    env_file:
      - ../.env
    ports:
      - "8081:8081"

  web:
    build: ../frontend
    environment:
      - VITE_API_URL=http://localhost:8081/api/v1
    ports:
      - "5174:5174"
    depends_on:
      - api
